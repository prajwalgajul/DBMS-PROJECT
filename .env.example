# Frontend API base URL.
# Leave blank for local development so the frontend uses the same origin.
VITE_API_BASE_URL=""

# Allowed frontend origin for cross-origin requests to the Render API.
# Example: https://your-project.vercel.app
CORS_ORIGIN=""
